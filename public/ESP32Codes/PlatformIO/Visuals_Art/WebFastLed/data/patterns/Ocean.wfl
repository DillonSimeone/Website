// Ocean waves
pos = index / pixelCount;
wave1 = sin(pos * 3 + time);
wave2 = sin(pos * 5 - time * 1.3);
combined = (wave1 + wave2) * 0.5 + 0.5;
hsv(0.55 + combined * 0.1, 0.7, 0.3 + combined * 0.7);
