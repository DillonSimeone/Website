# serial-magic-bridge — Cost-Down Analysis
This project contains Node.js code that runs on a host PC and bridges between USB Serial and the ESP-NOW ecosystem. There is no embedded firmware to port and no MCU to cost down. The companion firmware that talks to this bridge would be an espnow-sender variant on ESP32-C3.
